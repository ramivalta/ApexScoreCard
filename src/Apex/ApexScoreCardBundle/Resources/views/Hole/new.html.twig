{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hole creation</h1>

    <form action="{{ path('hole_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
         <input type="hidden" name="course_Id" value="{{course_Id}}"/>
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hole') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
