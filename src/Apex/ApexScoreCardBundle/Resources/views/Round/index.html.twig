{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Round list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Courseid</th>
                <th>Starttime</th>
                <th>Endtime</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('round_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.courseId }}</td>
                <td>{{ entity.startTime }}</td>
                <td>{{ entity.endTime }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('round_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('round_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('round_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
