{% extends '::base.html.twig' %}

{% block body -%}
    <h1>roundGolfer list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Roundid</th>
                <th>Golferid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('roundgolfer_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.roundId }}</td>
                <td>{{ entity.golferId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('roundgolfer_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('roundgolfer_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('roundgolfer_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
